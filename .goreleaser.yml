version: 2

project_name: figma-kit

builds:
  - main: ./cmd/figma-kit
    binary: figma-kit
    ldflags:
      - -s -w -X github.com/amine/figma-kit/internal/cli.Version={{.Version}}
    goos:
      - darwin
      - linux
      - windows
    goarch:
      - amd64
      - arm64
    ignore:
      - goos: windows
        goarch: arm64

archives:
  - format: tar.gz
    name_template: "{{ .ProjectName }}_{{ .Version }}_{{ .Os }}_{{ .Arch }}"
    format_overrides:
      - goos: windows
        format: zip

checksum:
  name_template: "checksums.txt"
  algorithm: sha256

changelog:
  sort: asc
  filters:
    exclude:
      - "^docs:"
      - "^test:"
      - "^ci:"
      - "^chore:"

brews:
  - repository:
      owner: amine
      name: homebrew-tap
    homepage: https://github.com/amine/figma-kit
    description: CLI for programmatic Figma design via the MCP server
    license: MIT
    install: |
      bin.install "figma-kit"
